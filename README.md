# cipher
A Python-based secure messaging tool that uses cryptographic encryption to protect messages. It generates and manages secret keys, encrypts messages into files, and safely decrypts them later, avoiding copy-paste errors through file-based storage and a simple terminal interface.
